# v5 — General catch-all

Skills imported wholesale from the "Forge" fork at
`/Users/donalmoloney/PycharmProjects/superpowers2/skills/` (excluding its internal
`v1/` rewrite subdirectory). 100 skills under `skills/`, copied verbatim and verified
with `diff -r`. `plugins/` is reserved for fork plugins if they get imported later.

This tier is a holding area, not a curated set:

- Skills keep their fork names, structure, and content as-is — including the fork's
  category folders (`debugging/`, `testing/`, `problem-solving/`, etc.).
- No tier discipline applies here. When a skill earns a real home, promote it:
  improvements to a core obra skill fold into `v1/`, supporting skills move to `v2/`,
  experiments to `v3/`, Karpathy/Cherny tools to `v4/` — following that tier's rules.
- A promoted skill should be removed from v5 so it lives in exactly one tier.
